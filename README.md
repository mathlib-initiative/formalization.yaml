# formalization.yaml

A self-reporting standard for autoformalization projects.

Drop `formalization.yaml` at the root of your repo. Fill in the `[required]` fields; the rest are optional. The point is to set an expectation that projects report on what was formalized, how, and how faithfully; without requiring everyone to invent their own format.
